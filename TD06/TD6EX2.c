#include <stdio.h>

int main(){
    float tabf[5};
    int tab[N];
    int tabEntiers[100] = {1,2,3,4,5};
    char alpha[26] = {'a', 'b', 'c', 'd', 'e'};

    tab[6] = -2;
    
    printf("%d", tab[i]);
    
   short T[7] = {3, -2, 1}; 
   printf("%d %d %d", T[0] T[1] T[2]); 

   for(i = 0; i<8; i++){
      printf("%d", T[i]);
   }
}
